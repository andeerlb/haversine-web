import { EnumGroupByPossibleRouter } from './enum-group-by-possible-router.model';

export interface GroupByPossibleRouter {
    value: EnumGroupByPossibleRouter;
    viewValue: string;
    enabled: Boolean
  }